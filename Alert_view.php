<?php
// This script and data application were generated by AppGini 5.84
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir=dirname(__FILE__);
	include("$currDir/defaultLang.php");
	include("$currDir/language.php");
	include("$currDir/lib.php");
	@include("$currDir/hooks/Alert.php");
	include("$currDir/Alert_dml.php");

	// mm: can the current member access this page?
	$perm=getTablePermissions('Alert');
	if(!$perm[0]) {
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout("window.location=\'index.php?signOut=1\'", 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = "Alert";

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = array(
		"`Alert`.`id`" => "id",
		"IF(    CHAR_LENGTH(`Customers1`.`Customer`), CONCAT_WS('',   `Customers1`.`Customer`), '') /* Customer */" => "Customer",
		"`Alert`.`Satus`" => "Satus",
		"`Alert`.`Comment`" => "Comment",
		"if(`Alert`.`Date`,date_format(`Alert`.`Date`,'%m/%d/%Y %h:%i %p'),'')" => "Date",
	);
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = array(
		1 => '`Alert`.`id`',
		2 => '`Customers1`.`Customer`',
		3 => 3,
		4 => 4,
		5 => '`Alert`.`Date`',
	);

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = array(
		"`Alert`.`id`" => "id",
		"IF(    CHAR_LENGTH(`Customers1`.`Customer`), CONCAT_WS('',   `Customers1`.`Customer`), '') /* Customer */" => "Customer",
		"`Alert`.`Satus`" => "Satus",
		"`Alert`.`Comment`" => "Comment",
		"if(`Alert`.`Date`,date_format(`Alert`.`Date`,'%m/%d/%Y %h:%i %p'),'')" => "Date",
	);
	// Fields that can be filtered
	$x->QueryFieldsFilters = array(
		"`Alert`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`Customers1`.`Customer`), CONCAT_WS('',   `Customers1`.`Customer`), '') /* Customer */" => "Customer",
		"`Alert`.`Satus`" => "Status",
		"`Alert`.`Comment`" => "Comment",
		"`Alert`.`Date`" => "Last update",
	);

	// Fields that can be quick searched
	$x->QueryFieldsQS = array(
		"`Alert`.`id`" => "id",
		"IF(    CHAR_LENGTH(`Customers1`.`Customer`), CONCAT_WS('',   `Customers1`.`Customer`), '') /* Customer */" => "Customer",
		"`Alert`.`Satus`" => "Satus",
		"`Alert`.`Comment`" => "Comment",
		"if(`Alert`.`Date`,date_format(`Alert`.`Date`,'%m/%d/%Y %h:%i %p'),'')" => "Date",
	);

	// Lookup fields that can be used as filterers
	$x->filterers = array('Customer' => 'Customer', );

	$x->QueryFrom = "`Alert` LEFT JOIN `Customers` as Customers1 ON `Customers1`.`id`=`Alert`.`Customer` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm[2]==0 ? 1 : 0);
	$x->AllowDelete = $perm[4];
	$x->AllowMassDelete = false;
	$x->AllowInsert = $perm[1];
	$x->AllowUpdate = $perm[3];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 0;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation["quick search"];
	$x->ScriptFileName = "Alert_view.php";
	$x->RedirectAfterInsert = "Alert_view.php?SelectedID=#ID#";
	$x->TableTitle = "CUSTOMER ALERT";
	$x->TableIcon = "resources/table_icons/Warning.png";
	$x->PrimaryKey = "`Alert`.`id`";

	$x->ColWidth   = array(  150, 150, 150, 150);
	$x->ColCaption = array("Customer", "Status", "Comment", "Last update");
	$x->ColFieldName = array('Customer', 'Satus', 'Comment', 'Date');
	$x->ColNumber  = array(2, 3, 4, 5);

	// template paths below are based on the app main directory
	$x->Template = 'templates/Alert_templateTV.html';
	$x->SelectedTemplate = 'templates/Alert_templateTVS.html';
	$x->TemplateDV = 'templates/Alert_templateDV.html';
	$x->TemplateDVP = 'templates/Alert_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HighlightColor = '#FFF0C2';
	$x->HasCalculatedFields = false;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, array('user', 'group'))) { $DisplayRecords = 'all'; }
	if($perm[2]==1 || ($perm[2]>1 && $DisplayRecords=='user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `Alert`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='Alert' and lcase(membership_userrecords.memberID)='".getLoggedMemberID()."'";
	}elseif($perm[2]==2 || ($perm[2]>2 && $DisplayRecords=='group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `Alert`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='Alert' and membership_userrecords.groupID='".getLoggedGroupID()."'";
	}elseif($perm[2]==3) { // view all
		// no further action
	}elseif($perm[2]==0) { // view none
		$x->QueryFields = array("Not enough permissions" => "NEP");
		$x->QueryFrom = '`Alert`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: Alert_init
	$render=TRUE;
	if(function_exists('Alert_init')) {
		$args=array();
		$render=Alert_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: Alert_header
	$headerCode='';
	if(function_exists('Alert_header')) {
		$args=array();
		$headerCode=Alert_header($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$headerCode) {
		include_once("$currDir/header.php"); 
	}else{
		ob_start(); include_once("$currDir/header.php"); $dHeader=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%HEADER%%>', $dHeader, $headerCode);
	}

	echo $x->HTML;
	// hook: Alert_footer
	$footerCode='';
	if(function_exists('Alert_footer')) {
		$args=array();
		$footerCode=Alert_footer($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$footerCode) {
		include_once("$currDir/footer.php"); 
	}else{
		ob_start(); include_once("$currDir/footer.php"); $dFooter=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%FOOTER%%>', $dFooter, $footerCode);
	}
?>